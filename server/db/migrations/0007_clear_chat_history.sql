-- 一次性清空智能问答会话历史（messages）。
-- 正式部署的库随试用累积了大量测试问答，上线时需统一清场；迁移每个库只执行一次，
-- 清掉后新产生的会话不受影响。全新空库先应用本迁移再灌种子，演示基线（丁一的
-- 预置对话）仍会写入；升级提问（escalations）与检索日志（kb_search_log）不属于
-- 问答历史，本次不动。
DELETE FROM messages;
